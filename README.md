# Metis Audit Anchors
External audit anchor log. Written by Watchdog (separate JVM, read-only for Metis).
Each anchor file = SHA-256 chain head at that timestamp. Tampering = detectable via hash-chain break.
