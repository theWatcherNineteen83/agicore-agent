// Evolution #1 — First successful mutation
// Date: 2026-05-22T20:16:30.869038280Z
// Model: qwen3.6:27b-q4_K_M
// Change: 2,2%
// ── Original ──
package de.agicore.modules.planner;

import de.agicore.kernel.action.ActionResult;
import de.agicore.kernel.goal.Goal;
import de.agicore.kernel.memory.Experience;
import de.agicore.kernel.meta.MetaCognition;
import de.agicore.kernel.planner.Planner;
import de.agicore.kernel.workspace.ContentItem;

import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.logging.Logger;

/**
 * Keyword-based planner — evolvable module v1.0.0.
 * <p>
 * Implements {@link Planner} (which extends {@link de.agicore.kernel.planner.EvolvableModule}).
 * The kernel depends only on the Planner interface; this implementation
 * lives in the evolvable modules package and can be mutated.
 * <p>
 * Mutation boundaries (enforced by SafetyGuard):
 * <ul>
 *   <li>Must NOT change Planner method signatures</li>
 *   <li>Must NOT add new dependencies</li>
 *   <li>Must NOT modify package name</li>
 *   <li>Must keep class name identical</li>
 *   <li>Max 15% code change per mutation</li>
 * </ul>
 */
public class StubPlanner implements Planner {

    private static final Logger LOG = Logger.getLogger(StubPlanner.class.getName());

    private static final int MIN_ATTEMPTS = 2;
    private static final double MIN_SUCCESS_RATE = 0.5;

    private final Map<String, Integer> planningSuccess = new ConcurrentHashMap<>();
    private final Map<String, Integer> planningAttempts = new ConcurrentHashMap<>();

    // ── EvolvableModule ────────────────────────────────────────
    private String version = "1.0.0";
    private double lastFitness = 0.0;

    @Override public String moduleName() { return "stub-planner"; }
    @Override public String version() { return version; }
    @Override public double lastFitness() { return lastFitness; }
    public void setLastFitness(double f) { this.lastFitness = f; }
    public void setVersion(String v) { this.version = v; }

    // ── Planner ────────────────────────────────────────────────

    @Override
    public List<String> plan(Goal goal, List<Experience> recentHistory,
                             List<ContentItem> broadcast, MetaCognition meta) {
        String desc = goal.description().toLowerCase();

        // Workspace broadcast influences planning
        for (ContentItem item : broadcast) {
            if ("world".equals(item.source()) && item.summary().contains("shell actions execute reliably")) {
                if (desc.contains("shell") || desc.contains("command") || desc.contains("system")) {
                    return List.of("shell");
                }
            }
            if ("world".equals(item.source()) && item.summary().toLowerCase().contains("http")) {
                if (desc.contains("http") || desc.contains("api") || desc.contains("request")) {
                    return List.of("http");
                }
            }
        }

        // Learned mappings
        String keyword = extractKeyword(desc);
        for (String action : List.of("shell", "http")) {
            String key = keyword + ":" + action;
            int attempts = planningAttempts.getOrDefault(key, 0);
            int successes = planningSuccess.getOrDefault(key, 0);
            if (attempts >= MIN_ATTEMPTS && (double) successes / attempts >= MIN_SUCCESS_RATE) {
                return List.of(action);
            }
        }

        // Keyword match
        if (desc.contains("shell") || desc.contains("command")) return List.of("shell");
        if (desc.contains("http") || desc.contains("api") || desc.contains("request")) return List.of("http");

        // Fallback
        for (var entry : planningAttempts.entrySet()) {
            if (entry.getKey().startsWith(keyword + ":") && entry.getValue() > 0) {
                String action = entry.getKey().substring(keyword.length() + 1);
                return List.of(action);
            }
        }

        return Collections.emptyList();
    }

    @Override
    public double expectedSuccess(Goal goal, String actionName) {
        String keyword = extractKeyword(goal.description().toLowerCase());
        String key = keyword + ":" + actionName;
        int attempts = planningAttempts.getOrDefault(key, 0);
        int successes = planningSuccess.getOrDefault(key, 0);
        return attempts > 0 ? (double) successes / attempts : 0.5;
    }

    @Override
    public void learnFromOutcome(Goal goal, List<String> plan, ActionResult result) {
        if (plan.isEmpty() || result == null) return;
        String keyword = extractKeyword(goal.description().toLowerCase());
        String action = plan.getFirst();
        String key = keyword + ":" + action;
        planningAttempts.merge(key, 1, Integer::sum);
        if (result.success()) planningSuccess.merge(key, 1, Integer::sum);
    }

    private String extractKeyword(String desc) {
        String[] words = desc.split("\\s+");
        for (String w : words) {
            if (!w.matches("^(a|an|the|to|for|in|on|at|of|and|or|run|send|check|get|do)$"))
                return w;
        }
        return words.length > 0 ? words[0] : "unknown";
    }

    public Map<String, Double> learnedSuccessRates() {
        Map<String, Double> rates = new LinkedHashMap<>();
        for (String key : planningAttempts.keySet()) {
            int att = planningAttempts.get(key);
            int succ = planningSuccess.getOrDefault(key, 0);
            rates.put(key, att == 0 ? 0.0 : (double) succ / att);
        }
        return rates;
    }
}

// ── Mutated ──
package de.agicore.modules.planner;

import de.agicore.kernel.action.ActionResult;
import de.agicore.kernel.goal.Goal;
import de.agicore.kernel.memory.Experience;
import de.agicore.kernel.meta.MetaCognition;
import de.agicore.kernel.planner.Planner;
import de.agicore.kernel.workspace.ContentItem;

import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.logging.Logger;

/**
 * Keyword-based planner — evolvable module v1.0.0.
 * <p>
 * Implements {@link Planner} (which extends {@link de.agicore.kernel.planner.EvolvableModule}).
 * The kernel depends only on the Planner interface; this implementation
 * lives in the evolvable modules package and can be mutated.
 * <p>
 * Mutation boundaries (enforced by SafetyGuard):
 * <ul>
 *   <li>Must NOT change Planner method signatures</li>
 *   <li>Must NOT add new dependencies</li>
 *   <li>Must NOT modify package name</li>
 *   <li>Must keep class name identical</li>
 *   <li>Max 15% code change per mutation</li>
 * </ul>
 */
public class StubPlanner implements Planner {

    private static final Logger LOG = Logger.getLogger(StubPlanner.class.getName());

    private static final int MIN_ATTEMPTS = 3;
    private static final double MIN_SUCCESS_RATE = 0.6;

    private final Map<String, Integer> planningSuccess = new ConcurrentHashMap<>();
    private final Map<String, Integer> planningAttempts = new ConcurrentHashMap<>();

    // ── EvolvableModule ────────────────────────────────────────
    private String version = "1.0.0";
    private double lastFitness = 0.0;

    @Override public String moduleName() { return "stub-planner"; }
    @Override public String version() { return version; }
    @Override public double lastFitness() { return lastFitness; }
    public void setLastFitness(double f) { this.lastFitness = f; }
    public void setVersion(String v) { this.version = v; }

    // ── Planner ────────────────────────────────────────────────

    @Override
    public List<String> plan(Goal goal, List<Experience> recentHistory,
                             List<ContentItem> broadcast, MetaCognition meta) {
        String desc = goal.description().toLowerCase();

        // Workspace broadcast influences planning
        for (ContentItem item : broadcast) {
            if ("world".equals(item.source()) && item.summary().contains("shell actions execute reliably")) {
                if (desc.contains("shell") || desc.contains("command") || desc.contains("system")) {
                    return List.of("shell");
                }
            }
            if ("world".equals(item.source()) && item.summary().toLowerCase().contains("http")) {
                if (desc.contains("http") || desc.contains("api") || desc.contains("request")) {
                    return List.of("http");
                }
            }
        }

        // Learned mappings
        String keyword = extractKeyword(desc);
        for (String action : List.of("shell", "http")) {
            String key = keyword + ":" + action;
            int attempts = planningAttempts.getOrDefault(key, 0);
            int successes = planningSuccess.getOrDefault(key, 0);
            if (attempts >= MIN_ATTEMPTS && (double) successes / attempts >= MIN_SUCCESS_RATE) {
                return List.of(action);
            }
        }

        // Keyword match
        if (desc.contains("shell") || desc.contains("command")) return List.of("shell");
        if (desc.contains("http") || desc.contains("api") || desc.contains("request")) return List.of("http");

        // Fallback
        for (var entry : planningAttempts.entrySet()) {
            if (entry.getKey().startsWith(keyword + ":") && entry.getValue() > 0) {
                String action = entry.getKey().substring(keyword.length() + 1);
                return List.of(action);
            }
        }

        return Collections.emptyList();
    }

    @Override
    public double expectedSuccess(Goal goal, String actionName) {
        String keyword = extractKeyword(goal.description().toLowerCase());
        String key = keyword + ":" + actionName;
        int attempts = planningAttempts.getOrDefault(key, 0);
        int successes = planningSuccess.getOrDefault(key, 0);
        return attempts > 0 ? (double) successes / attempts : 0.5;
    }

    @Override
    public void learnFromOutcome(Goal goal, List<String> plan, ActionResult result) {
        if (plan.isEmpty() || result == null) return;
        String keyword = extractKeyword(goal.description().toLowerCase());
        String action = plan.getFirst();
        String key = keyword + ":" + action;
        planningAttempts.merge(key, 1, Integer::sum);
        if (result.success()) planningSuccess.merge(key, 1, Integer::sum);
    }

    private String extractKeyword(String desc) {
        String[] words = desc.split("\\s+");
        for (String w : words) {
            if (!w.matches("^(a|an|the|to|for|in|on|at|of|and|or|run|send|check|get|do|with|using|by)$"))
                return w;
        }
        return words.length > 0 ? words[0] : "unknown";
    }

    public Map<String, Double> learnedSuccessRates() {
        Map<String, Double> rates = new LinkedHashMap<>();
        for (String key : planningAttempts.keySet()) {
            int att = planningAttempts.get(key);
            int succ = planningSuccess.getOrDefault(key, 0);
            rates.put(key, att == 0 ? 0.0 : (double) succ / att);
        }
        return rates;
    }
}
